section.page.logout
  h2 Welcome to the Sociomantic pool table Leaderboard — The Pool Party!

  div(data-hook="auth-form")
    h2 Log out
    form.form-signin(role="logout-form")
      fieldset(data-hook="field-container")
      button.btn.btn-lg.btn-primary.btn-block(type="submit") Sign out

